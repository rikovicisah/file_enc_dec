{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Enkriptor",
      "type": "cppdbg",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "args": [
        "ulaz.txt",
        "izlaz.bin"
      ],
      "stopAtEntry": false,
      "cwd": "${fileDirname}",
      "preLaunchTask": "C/C++: gcc build active file",
      "MIMode": "gdb",
      "setupCommands": [
        {
          "description": "Omogući printanje u boji",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ]
    }
  ]
}
